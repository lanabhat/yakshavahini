# Signing keys — never commit. android-release.keystore is the real
# release key; its password lives only in a password manager (see
# README.md in this folder), never in the repo.
*.keystore
*.jks

node_modules

# Bubblewrap-generated Android project — regenerate with `npx bubblewrap build`
app/build
build
.gradle
local.properties
*.apk
*.apk.idsig
*.aab
